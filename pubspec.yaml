name: tflite_text_extraction
description: A new Flutter project.
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.1.1 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  # Updated dependencies
  camera: ^0.12.0+1
  camera_windows: ^0.2.6+4
  image: ^4.0.17
  file_picker: ^10.3.10
  cupertino_icons: ^1.0.9
  tflite_flutter: ^0.12.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/lite-model_east-text-detector_fp16.tflite
    - assets/wizardiusbewebicon.png